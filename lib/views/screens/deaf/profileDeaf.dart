import 'package:flutter/material.dart';

class ProfileDeaf extends StatelessWidget {
  const ProfileDeaf({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}