import 'package:flutter/material.dart';

class ChatDeaf extends StatelessWidget {
  const ChatDeaf({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}